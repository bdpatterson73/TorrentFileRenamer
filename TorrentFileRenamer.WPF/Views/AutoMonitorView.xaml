<UserControl x:Class="TorrentFileRenamer.WPF.Views.AutoMonitorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:TorrentFileRenamer.WPF.ViewModels"
             xmlns:models="clr-namespace:TorrentFileRenamer.WPF.Models"
             xmlns:converters="clr-namespace:TorrentFileRenamer.WPF.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800"
             d:DataContext="{d:DesignInstance Type=vm:AutoMonitorViewModel}">

    <UserControl.Resources>
        <!-- Activity Status Colors -->
        <SolidColorBrush x:Key="SuccessBrush" Color="#28A745" />
        <SolidColorBrush x:Key="FailedBrush" Color="#DC3545" />

        <!-- Converters -->
        <converters:CountToVisibilityConverter x:Key="CountToVisibilityConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />

        <!-- Status Indicator Style -->
        <Style x:Key="StatusIndicator" TargetType="Ellipse">
            <Setter Property="Width" Value="12" />
            <Setter Property="Height" Value="12" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <ToolBar Grid.Row="0" Style="{StaticResource ModernToolBar}">
            <Button Content="Configure"
                    Command="{Binding ConfigureCommand}"
                    Style="{StaticResource PrimaryButton}"
                    ToolTip="Configure folder monitoring settings" />
            <Separator />
            <Button Content="Start Monitoring"
                    Command="{Binding StartCommand}"
                    Style="{StaticResource PrimaryButton}"
                    ToolTip="Start folder monitoring" />
            <Button Content="Stop Monitoring"
                    Command="{Binding StopCommand}"
                    Style="{StaticResource SecondaryButton}"
                    ToolTip="Stop folder monitoring" />
            <Separator />
            <Button Content="Clear Log"
                    Command="{Binding ClearLogCommand}"
                    Style="{StaticResource SecondaryButton}"
                    ToolTip="Clear activity log" />
        </ToolBar>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- Configuration & Status Panel -->
            <Border Grid.Row="0"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Background="White"
                    CornerRadius="4"
                    Padding="15"
                    Margin="0,0,0,10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <!-- Configuration Info -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Monitoring Configuration"
                                   Style="{StaticResource HeadingTextBlock}"
                                   Margin="0,0,0,10" />

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="120" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Watch Folder:" FontWeight="SemiBold"
                                       Margin="0,0,0,5" />
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding WatchFolder}"
                                       TextTrimming="CharacterEllipsis" Margin="0,0,0,5"
                                       ToolTip="{Binding WatchFolder}" />

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Destination:" FontWeight="SemiBold"
                                       Margin="0,0,0,5" />
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DestinationFolder}"
                                       TextTrimming="CharacterEllipsis" Margin="0,0,0,5"
                                       ToolTip="{Binding DestinationFolder}" />

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Extensions:" FontWeight="SemiBold"
                                       Margin="0,0,0,5" />
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding FileExtensions}" Margin="0,0,0,5" />

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Stability Delay:" FontWeight="SemiBold" />
                            <TextBlock Grid.Row="3" Grid.Column="1">
                                <Run Text="{Binding StabilityDelay}" />
                                <Run Text="seconds" />
                            </TextBlock>
                        </Grid>
                    </StackPanel>

                    <!-- Status Indicator -->
                    <Border Grid.Column="1"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            Background="#F8F9FA"
                            CornerRadius="4"
                            Padding="20"
                            MinWidth="180"
                            Margin="15,0,0,0">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock Text="Status"
                                       FontSize="12"
                                       Foreground="Gray"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,10" />

                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Ellipse Width="12" Height="12" Margin="0,0,8,0" VerticalAlignment="Center">
                                    <Ellipse.Style>
                                        <Style TargetType="Ellipse">
                                            <Setter Property="Fill" Value="#6C757D" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CurrentStatus}"
                                                             Value="{x:Static models:MonitoringStatus.Running}">
                                                    <Setter Property="Fill" Value="#28A745" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CurrentStatus}"
                                                             Value="{x:Static models:MonitoringStatus.Starting}">
                                                    <Setter Property="Fill" Value="#FFC107" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CurrentStatus}"
                                                             Value="{x:Static models:MonitoringStatus.Stopping}">
                                                    <Setter Property="Fill" Value="#FFC107" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding CurrentStatus}"
                                                             Value="{x:Static models:MonitoringStatus.Error}">
                                                    <Setter Property="Fill" Value="#DC3545" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Ellipse.Style>
                                </Ellipse>
                                <TextBlock Text="{Binding StatusText}"
                                           FontSize="18"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center" />
                            </StackPanel>

                            <CheckBox Content="Auto-start on load"
                                      IsChecked="{Binding AutoStart}"
                                      Margin="0,15,0,0"
                                      HorizontalAlignment="Center" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Recent Activity Log -->
            <Border Grid.Row="1"
                    BorderBrush="{StaticResource BorderBrush}"
                    BorderThickness="1"
                    Background="White"
                    CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <!-- Header -->
                    <Border Grid.Row="0"
                            Background="#F8F9FA"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="10">
                        <TextBlock Text="Recent Activity"
                                   Style="{StaticResource HeadingTextBlock}" />
                    </Border>

                    <!-- Activity DataGrid -->
                    <DataGrid Grid.Row="1"
                              x:Name="ActivityDataGrid"
                              ItemsSource="{Binding RecentActivity}"
                              AutoGenerateColumns="False"
                              IsReadOnly="True"
                              SelectionMode="Single"
                              CanUserSortColumns="True"
                              CanUserResizeColumns="True"
                              CanUserReorderColumns="True"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              AlternationCount="2">

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                                        <Setter Property="Background" Value="#FFFDECEA" />
                                        <Setter Property="Foreground" Value="#721C24" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </DataGrid.RowStyle>

                        <DataGrid.Columns>
                            <!-- Timestamp -->
                            <DataGridTextColumn Header="Time"
                                                Binding="{Binding FormattedTimestamp}"
                                                Width="140" />

                            <!-- File Name -->
                            <DataGridTextColumn Header="File"
                                                Binding="{Binding FileName}"
                                                Width="*"
                                                MinWidth="150" />

                            <!-- Activity Type -->
                            <DataGridTextColumn Header="Activity"
                                                Binding="{Binding ActivityType}"
                                                Width="100" />

                            <!-- Status with indicator -->
                            <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal">
                                            <Ellipse Width="8" Height="8"
                                                     Margin="0,0,5,0"
                                                     VerticalAlignment="Center">
                                                <Ellipse.Style>
                                                    <Style TargetType="Ellipse">
                                                        <Setter Property="Fill" Value="#DC3545" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSuccess}" Value="True">
                                                                <Setter Property="Fill" Value="#28A745" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Ellipse.Style>
                                            </Ellipse>
                                            <TextBlock Text="{Binding Status}"
                                                       VerticalAlignment="Center" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <!-- Message -->
                            <DataGridTextColumn Header="Message"
                                                Binding="{Binding Message}"
                                                Width="*"
                                                MinWidth="200" />
                        </DataGrid.Columns>
                    </DataGrid>

                    <!-- Empty State -->
                    <StackPanel Grid.Row="1"
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Visibility="{Binding RecentActivity.Count, Converter={StaticResource CountToVisibilityConverter}}">
                        <TextBlock Text="No activity yet"
                                   FontSize="16"
                                   Foreground="Gray"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Configure and start monitoring to see activity"
                                   FontSize="12"
                                   Foreground="Gray"
                                   HorizontalAlignment="Center"
                                   Margin="0,5,0,0" />
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Style="{StaticResource ModernStatusBar}">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
            <StatusBarItem HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Activity Entries: " />
                    <TextBlock Text="{Binding RecentActivity.Count}" />
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>